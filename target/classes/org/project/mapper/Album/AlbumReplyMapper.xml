<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.project.mapper.Album.AlbumReplyMapper">
	<!-- 댓글 등록 -->	
	<insert id="insert">
		insert into album_reply (rno, ano, reply, replyer) values (seq_album_reply.nextval, #{ano}, #{reply}, #{replyer})
	</insert>
	
	
	<!-- 댓글 조회 -->
	<select id="read" resultType="org.project.domain.Album.AlbumReplyVO">
		select * from album_reply where rno=#{rno}
	</select>
	
	<!-- 댓글 목록 -->
	<select id="getListWithPaging" resultType="org.project.domain.Album.AlbumReplyVO">
		<![CDATA[
			select rno, ano, reply, replyer, replydate, updatedate 
			from (
				select /*+INDEX(reply idx_album_reply)*/
					rownum rn, rno, ano, reply, replyer, replydate, updatedate
					from album_reply where ano=#{ano} and rno>0 and rownum <= #{cri.pageNum} * #{cri.amount} 
			) where rn > (#{cri.pageNum} -1) * #{cri.amount}		
		]]>
	</select>	
	
	<!-- 특정 게시글에 달린 댓글 개수 세기 -->
	<select id="getCountByAno" resultType="int">
		select count(rno) from album_reply where ano=#{ano}	
	</select>
	
	
	<!-- 댓글 수정 -->
	<update id="update">
		update album_reply set reply=#{reply}, updatedate=sysdate where rno=#{rno} 
	</update>
	
	
	<!-- 댓글 삭제 -->
	<delete id="delete">
		delete from album_reply where rno=#{rno}
	</delete>	
</mapper>